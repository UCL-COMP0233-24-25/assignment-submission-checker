[tox]
env_list =
    py310
minversion = 4.6.3

[testenv]
description = Run unit tests with pytest
package = wheel
passenv = HOME
wheel_build_env = .pkg
deps =
    pytest>=6
commands =
    pytest {tty:--color=yes} -vv {toxinidir} {posargs}

[gh-actions]
python =
    3.10: py310
